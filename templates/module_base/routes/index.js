// Placeholder para rutas propias del módulo.
// En esta plantilla, se usa UI dentro de index.js, pero
// si más adelante migras a un router, este archivo ya existe.

export function registerRoutes(router) {
  // router.add('/__MODULE_NAME__', () => { ... })
}